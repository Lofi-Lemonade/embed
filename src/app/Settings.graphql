query Settings($guild: String!) {
  settings(id: $guild) {
    guestMode
    invite
    inviteButtonText
    singleChannel
    hideSidebar
    readonly
    filesEnabled
    isCustomAuthEnabled
    theme {
      css
      colors {
        primary
        accent
        background
      }
    }
  }
}
